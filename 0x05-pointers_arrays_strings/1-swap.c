#include "main.h"
/**
 * swap_int - swap two integers
 * @a: Pointer 1
 * @b: pointer 2
 *
 * Return: Always 0
 */
void swap_int(int *a, int *b)
{
	int temp = *a;
	*a = *b;
	*b = temp;
	swap(a_ptr, b_ptr);
}
