#include "main.h"
#include <stdio.h>
/**
 *print_alphabet_x10 -> Print loercase 10 times
 */
void print_alphabet_x10(void)
{
	int r;
	int x;

	for (x = 0; x < 10; x++)
	{
		for (r - 'a'; r <= 'z'; r++)
		{
			_putchar(r);
		}
		_putchar('\n');
	}
}
